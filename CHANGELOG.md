# 喵日常 · 更新日志

> Mia's Cute Planner 是咪咪大人的第一个 vibe coding 学习项目，从零基础开始，跟着 AI 结对编程一点点做出来。
> 这里是它的"成长日记"。

---

## 🌸 v1.0.0 (2026-09-11) · MVP 正式达成

这是「喵日常」的第一个正式版本。所有核心功能跑通了、视觉风格定下来了、历史快照可以看了 —— 一个真正可以用、能用的日常计划本网页。

### ✨ 核心功能

- **📝 今日待办**
  - 输入框回车添加任务
  - 点击 checkbox 立即划线完成
  - hover 显示 × 删除按钮
  - 实时显示"已完成 / 总数"进度
  - 列表内部滚动（任务再多也不撑长页面）
  - **重复检测**：添加重复项时弹出可爱的 🥺 toast 提示

- **✨ 习惯打卡**
  - 4 个默认习惯：💧 喝水 / 📖 阅读 / 🛌 早睡 / 🏃 运动
  - 输入框可新增任意习惯（16 个 emoji 选择 + 「不选」自动给 ⭐）
  - hover 显示 × 删除按钮
  - 实时显示"完成数 / 总数"
  - 打卡状态按日期保存，隔天各自独立

- **💛 今日心情**
  - 10 个心情 emoji 横向滑动选择（😊😌😴🥺🥰😡😰😎🎉🤔）
  - 选中态：白玻璃底 + 橙色细外圈光环
  - 一句话日记输入框（auto-resize、自动保存）
  - emoji 和文字**各自独立保存**，可以只填一个

- **📅 看历史**（新增！原本不在 MVP 里）
  - 主视图顶部"📅 看历史"按钮一键切换
  - 进入历史默认看昨天，避免和今天混淆
  - 当月日历网格，点选任意过去某天查看当天数据
  - 今天在历史视图里"不可选"（标注"今天" + 灰显）
  - 日历默认展开，可点击卡片头部"收起 ▲"
  - 历史模式下三个模块全部**只读**：表单隐藏、checkbox disabled、emoji 不可点、textarea 彻底 lock

### 🎨 视觉风格

- **配色**：奶油米色底 + 三色卡片染色（待办蓝 / 习惯粉 / 心情橘）
- **背景**：4 色径向渐变 + 两个超大模糊圆**缓慢漂浮**
- **质感**：所有卡片 `backdrop-filter: blur(24px)` 磨砂玻璃
- **插画**：手绘 SVG 圆滚滚 Chiikawa 风小猫咪浮在 hero 区
- **字体**：中文 PingFang SC，英文 Google Fonts Nunito
- **动效**：卡片渐入、背景圆点漂浮、心情 emoji hover 缩放
- **首屏**："今天也要好好生活呀～" 一句小温暖

### 🔧 技术实现

- 单 HTML + CSS + JS 三文件，无构建工具
- 所有数据存浏览器 **LocalStorage**，换浏览器会丢（同浏览器不丢）
- todo 数据按日期分组 `{ "2026-09-11": [...todos] }`
- habit 数据全局存 + 打卡按天存 `{ "2026-09-11": ["water","read"] }`
- mood 数据按天存 `{ "2026-09-11": { mood: "happy", note: "..." } }`

### ⚠️ V1.0.0 已知限制

- **不能跨设备**：数据只存在本浏览器，换电脑/手机看不到
- **不能云备份**：清浏览器缓存会丢数据（V2.0 解决）
- **不能导出**：没有 JSON 导出/导入入口（V1.5 加）
- 不能多用户协作

### 🚀 下一步

- **V1.0.1**：把代码 push 到 GitHub + 部署 Vercel，生成可分享链接
- **V1.1.0**（开发中）：接入 Supabase，加邮箱魔法链接登录 + 跨设备云端同步
- **V1.2**：JSON 导出/导入（防数据丢失的简易备份方案）
- **V1.5**：周/月视图、习惯自定义编辑、连续打卡统计
- **V2.0**：预留更大的功能演进（如好友协作、分享等）

---

## 🌸 v1.1.2 (2026-09-12) · 移动端登录攻坚 + 国内可访问（已上线）

> 状态：✅ 已上线。生产地址：`https://miao-daily-miao-daily-d1gmyiugua09ec60d.webapps.tcloudbase.com`（腾讯云 CloudBase 静态托管）。
> 一句话总结：**从「手机根本打不开」到「手机能登录」，踩了 6 个连环坑，全部定位到根因并解决。**

### 🎯 这次解决的事

v1.1.1 在电脑上一切正常，但**手机完全用不了**。本版把移动端彻底打通：

| 设备 | 登录方式 | 状态 |
|------|---------|------|
| 💻 电脑 | 填邮箱 → 点邮件里的魔法链接 → 直接进 | ✅ 一直都正常 |
| 📱 手机 | 填邮箱 → 长按邮件按钮「拷贝链接地址」→ 粘到登录页蓝色区域 → 进 | ✅ 本版攻克 |

### 🕳️ 踩过的 6 个坑（按发现顺序）

1. **CDN 被墙**：jsdelivr 上的 Supabase SDK 在国内手机加载不到 → 改为**自托管** SDK（`src/vendor/supabase-js.js`）
2. **Vercel 域名被 DNS 污染**：手机报 `ERR_CONNECTION_ABORTED`，域名被解析到 Facebook IP，换流量无效 → **整体迁到腾讯云 CloudBase 静态托管**
3. **SDK 在手机卡死**：`setSession()` 内部 `await initializePromise` 在手机上永不返回 → **彻底绕开 SDK**，自己读写会话
4. **代理返回压缩乱码**：SCF 云函数把 gzip 响应当字符串转码，二进制被压坏（`1f efbfbd 08`），JSON 解析必败 → 登录改为**本地解码 JWT**，一个网络请求都不发
5. **项目 ID 取不到**：原代码从 API 地址正则提取 `supabase.co`，换成代理地址后匹配为空 → 改为**从 anonKey 的 JWT payload 解出** `ref`
6. **手机丢凭证（终极 Boss）**：邮件里的链接只是 `supabase.co/auth/v1/verify`，凭证要 302 跳转后才出现在网址 `#` 后面；而手机的邮件 App 内置浏览器 + 腾讯「确定访问」中间页会把 `#` 后面**整个吃掉** → 新增**粘贴链接登录**通道

### ✨ 新增能力

- **🔗 粘贴链接登录**（`src/linklogin.js`，核心新文件）
  - 通道一：链接直接带 `access_token` → 本地拆 JWT 取用户 ID，秒登
  - 通道二：粘邮件里的 `supabase.co/auth/v1/verify?token=…` 验证链接 → **在当前页面内代为完成验证跳转**，302 回跳的凭证直接被本页接住，全程一个标签页
  - 登录页和主页遮罩**两处入口**都可用
  - 💡 implicit 凭证点过一次不会作废，所以「先点了没进去、再复制粘贴」照样有效
- **📮 发送魔法链接绕开 SDK**（`src/auth.js`）：自己 `fetch POST /auth/v1/otp`，只认状态码不解析响应体 —— 网关乱码再也影响不到
- **🩺 首页自检报告**：识别不到凭证时**不再默默跳回登录页**，而是在遮罩上写出网址实况（路径 / `?` 后 / `#` 后长度），2.5 秒就亮出来
- **🚪 退出按钮不再卡死**：原来在等 SDK 的 `signOut()`（永不返回），改成先清本地会话立刻跳转
- **🔧 运维后门**：`login.html?force=1` 强制停在登录页不被自动弹走
- **🔄 凭证读取兼容**：`#` 和 `?` 两种写法都认（部分邮箱 App 会把 `#` 改写成 `?`）

### 🛠️ 部署架构（本版确立）

```
手机/电脑浏览器
   ↓
腾讯云 CloudBase 静态托管（src/ 下的静态文件）
   ↓ /sb/*
腾讯云 SCF 云函数 supabase-proxy（tencent-scf/supabase-proxy/）
   ↓ https
Supabase（Auth + PostgreSQL + RLS）
```

- 静态文件改完 → 控制台「文件管理」直接覆盖上传（**这种方式不产生版本记录，「未创建版本」是正常状态**）
- 每次改线上文件要**同步升资源版本号** `?v=N`（当前 `?v=11`），否则移动端缓存会让修复"看起来没生效"
- Supabase 后台 Site URL / Redirect URLs 必须填 CloudBase 域名，否则邮件链接会跳回 localhost

### ⚠️ 已知限制（留给以后）

1. **手机点链接仍会回登录页**：根因是腾讯默认域名的「确定访问」中间页 + 邮件 App 内置浏览器丢弃 `#` 后凭证。绕过方式是粘贴链接。**彻底解法**：① 域名 ICP 备案去掉中间页；② 改 6 位验证码登录（邮件模板换 `{{ .Token }}`），不依赖跳转
2. **云端数据同步在手机可能不灵**：修好压缩 bug 的云函数包（`tencent-scf/supabase-proxy.zip`）尚未部署
3. **「确定访问」中间页**：等 ICP 备案通过后消失，不影响使用

### 📚 完整排障记录

见 `docs/DEPLOYMENT.md`（本版新建），把 6 个坑的现象、排查命令、根因、解法全部存档了。

---

## 🌸 v1.1.1 (2026-09-11) · 体验修复与小优化（已上线）

> 状态：✅ 已上线部署。生产地址：`https://learn-vibe-coding-miao-daily.vercel.app`（Vercel 托管，Supabase 云端登录，任何人邮箱可注册、数据按账号隔离）。

### 🐛 修复与优化
- **退出登录**：补上退出按钮事件绑定 + 确认弹窗（之前点击无反应）
- **待办编辑**：点文字进入编辑态（回车保存 / Esc 取消），并修复编辑框回车后不消失的 bug
- **字数提示**：待办最多 50 字、习惯名最多 12 字、心情最多 200 字，超过时弹可爱提示
- **待办排序**：未完成的待办自动置顶，已完成的排到后面
- **历史习惯**：历史视图显示完整习惯列表，未打卡的灰显

---

## 🌸 v1.1.0 (2026-09-11) · 云端登录同步版（已上线）

> 状态：已实现并验证通过（邮箱魔法链接登录 + 跨设备云端同步跑通）。本次把原本规划的 v2.0 重新编号为 v1.1.0（咪咪大人拍板：云端登录是已有产品的增强，不是大版本跳跃）。

### 🎯 这次要做的

- **📮 邮箱魔法链接登录**：输入邮箱 → 收邮件 → 点链接自动登录（首次=自动注册）
- **☁️ 数据上云**：待办 / 习惯 / 心情从 LocalStorage 搬到 Supabase 云端数据库
- **📱 跨设备同步**：A 设备写，B 设备登录能看到同一份数据
- **🔒 数据隔离**：每条数据带 user_id，RLS 行级安全，朋友之间互不可见
- **📦 一次性导入**：登录后检测旧 LocalStorage 数据，黄色提示条一键迁移到云端

### 🛠️ 技术栈

- 前端：HTML/CSS/JS（沿用 v1.0）
- BaaS：Supabase（免费版）
- Auth：Supabase Auth 邮箱魔法链接
- 数据库：PostgreSQL（单表 `user_data`：`user_id` 主键 + `payload` jsonb + `updated_at`，配合 RLS 行级安全 `auth.uid() = user_id`；前端 LocalStorage 作离线缓存 + 800ms 防抖上传云端）

---

## v1.0.0 之前的小步迭代（备忘）

为了不丢这一路的小进步，按时间顺序记录一下：

| 版本 | 主要改动 |
|------|---------|
| v0.1 | 第一份静态 HTML+CSS 原型，3 个模块卡片可见但没 JS |
| v0.2 | 加 JS 交互 + 磨砂玻璃背景 + SVG 小猫插画 |
| v0.3 | 待办框内部滚动 + 习惯可新增删除 + SVG 图标 + 心情横滑 |
| v0.4 | 咪咪大人反馈 SVG 图标"丑" → 全部倒退回 emoji |
| v0.5 | 习惯 emoji 选择器（16 个，分两行）+ 心情 emoji 加到 10 个 |
| v0.6 | 历史快照（calendar 组件 + ViewController） |
| v0.7 | 待办/习惯添加去重 + 顶部醒目 toast 提示 🥺✨ |

> 整个开发过程 + PRD + memory 都已经存在 git 里，所有决定可追溯。
